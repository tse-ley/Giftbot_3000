{% extends 'base.html.twig' %}

{% block title %}Gift Management{% endblock %}

{% block body %}

<style>
    .gift-wrapper {
        margin: 2em auto;
        max-width: 900px;
        width: 95%;
        font: 16px/1.5 sans-serif;
    }
    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 1em;
    }
    th, td {
        border: 1px solid #ccc;
        padding: 10px;
        text-align: left;
    }
    th {
        background-color: #f0f0f0;
    }
    .gift-count {
        font-weight: bold;
        margin-bottom: 1em;
    }
    .actions a {
        margin-right: 10px;
        color: #007bff;
        text-decoration: none;
    }
    .actions a.delete {
        color: red;
    }
    .add-btn {
        display: inline-block;
        padding: 8px 16px;
        background-color: #28a745;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin-bottom: 10px;
    }
</style>

<div class="gift-wrapper">
    <h1>🎁 Gift List</h1>
        <a href="{{ path('admin_dashboard', { slash: '/' }) }}" class="add-btn" style="background-color: #fc6ea0; margin-bottom: 20px;">⬅️ Back to Dashboard</a>

    <div class="gift-count">
        Total Gifts: {{ gifts|length }}
    </div>

    <a href="{{ path('gift_add') }}" class="add-btn">➕ Add New Gift</a>

    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Description</th>
                <th>Created At</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for gift in gifts %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ gift.name }}</td>
                    <td>{{ gift.description }}</td>
                    <td>{{ gift.createdAt|date('Y-m-d H:i') }}</td>
                    <td class="actions">
                        <a href="{{ path('gift_delete', { id: gift.id }) }}" class="delete" onclick="return confirm('Are you sure you want to delete this gift?');">🗑 Delete</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">No gifts found.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
